import mockedQuestions from "./mockedQuestions";
import mockedScore from "./mockedScore";

// defined the scope of useQuiz mocks
export const logScore = (answer: boolean) => {};
export const score = mockedScore;
export const isLoading = true;
export const resetScore = () => {};
export const loadQuizQuestions = () => {};
export const questions = mockedQuestions;
